language: python
python:
  - "3.5"
  - "3.6"
install: pip install tox-travis codecov
jobs:
  include:
    - stage: test
      script: tox
    - stage: lint
      script: tox -e lint
after_success:
  - tox -e coverage
  - codecov
